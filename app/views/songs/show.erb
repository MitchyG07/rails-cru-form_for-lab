<h1><%= @song.name %></h1>
<div><%= link_to Artist.find(@song.artist_id).name, artist_path(@song.artist_id)%></div>
<div><%= link_to Genre.find(@song.genre_id).name, genre_path(@song.genre_id) %></div>
